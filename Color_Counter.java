public void run(ImageProcessor ip) {
		int red = 0;
		int blue = 0;
		int green = 0;
		int white = 0;
		int black = 0;
		int[] pixels = (int[])ip.getPixels();
		for (int i=0; i<pixels.length; i++)
			if(pixels[i] = int((0xff0000)>>16){
				red++;
			}
			if(pixels[i] = int((0x00ff00)>>8){
				green++;
			}
			if(pixels[i] = int((0x0000ff)){
				blue++;
			}
			if(pixels[i] = int((0xff0000)>>16){
				red++;
			}
			if(pixels[i] = 0){
				black++;
			}
			if(pixels[i] = int((0xffffff)){
				red++;
			}
			
			
		}
	}